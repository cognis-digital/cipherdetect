services:
  cipherdetect:
    build: .
    image: cognis/cipherdetect:local
    restart: unless-stopped
    ports: ["8000:8000"]
    environment:
      - COGNIS_LOG_LEVEL=info
    volumes: ["./data:/data"]
